~extends: "@WexampleSymfonyLoaderBundle.config.design_system.app.index"

page_title: Controls
page_icon: ph:bold/cursor-click

intro: "Interactive controls for triggering operations and capturing user input. Buttons, links, menus, selects, and switches cover the full spectrum of actions — from primary calls-to-action to assistive inputs that adapt to context, state, and layout."

section:
  compound:
    title: Compound usage

button:
  primary: Primary button

select:
  option_one: Option one
  option_two: Option two
  option_three: Option three

menu:
  primary: Menu button
  item:
    report: Report
    settings: Settings
    logout: Log out
    long: With a quite, quite, quite, quite long title

switch:
  label_right: Enable feature

link:
  external: External link
