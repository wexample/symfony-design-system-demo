~extends: "@WexampleSymfonyLoaderBundle.config.design_system.app.index"

page_title: Layout
page_icon: ph:bold/squares-four

intro: "Structural primitives for arranging content on screen. Blocks, stacks, zones, toolbars, tabs, tables, and media layouts are the building blocks of every interface — they define how space is divided, how elements are grouped, and how information flows."

section:
  compound:
    title: Compound usage

compound:
  block:
    title: Pipeline projection
    status: Ready
    description: Blocks compose with toolbars, tabs, and zones — this is the typical structure of a feature panel.
  tab:
    one: Overview
    two: Activity
    three: Settings
  button:
    create: Create
    refresh: Refresh
    export: Export
